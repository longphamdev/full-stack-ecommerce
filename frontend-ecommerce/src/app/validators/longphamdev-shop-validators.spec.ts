import { LongphamdevShopValidators } from './longphamdev-shop-validators';

describe('LongphamdevShopValidators', () => {
  it('should create an instance', () => {
    expect(new LongphamdevShopValidators()).toBeTruthy();
  });
});
